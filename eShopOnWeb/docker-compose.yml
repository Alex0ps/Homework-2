version: '3.4'

services:
  eshopwebmvc:
    image: ${DOCKER_REGISTRY-}eshopwebmvc
    build:
      context: .
      dockerfile: src/Web/Dockerfile
    ports:
          - "80:80"
    depends_on:
            - db
  db:
        image: "mcr.microsoft.com/mssql/server:2017-latest"
        container_name: sqldb
        environment:
            SA_PASSWORD: "0147852369Main"
            ACCEPT_EULA: "Y"
        ports:
          - "1433:1433"
